# ハッキング風パズルゲーム 画面設計書 v1.1

> 前提: [Overview.md](Overview.md) のゲーム設計（コマンド仕様・パズルパターン）に準拠する。
> 特に **「自由入力ゼロ」「常時ステータスバーを持たない」** の2点は全画面で厳守する。

> v1.1 変更点: 5コマンドを一括で教える専用チュートリアル画面を廃止。
> 「大枠のチュートリアル＝ステージ1-1を実際にプレイ＋ホーム画面ガイド」と
> 「各コマンドの説明＝各ステージで初出時にインラインヒント」の二段構成に変更。

---

## 画面遷移図（全体フロー）

```
┌─────────┐  初回起動  ┌────────────────┐  クリア  ┌───────────────┐
│ タイトル │ ─────────→ │  ステージ1-1     │ ───────→ │ ホーム画面ガイド │
└────┬────┘            │ （大枠チュートリアル）│         │ （初回のみ）    │
     │                 └────────────────┘         └───────┬───────┘
     │ 2回目以降                                            │
     ↓                                                     ↓
                          ┌───────────┐ ←──────────────────┘
                          │  ホーム画面  │←───────────────┐
                          └─────┬─────┘                │
                                │ ステージ選択              │
                                ↓                       │
                        ┌───────────────┐               │
                        │ ストーリー画面   │               │ ホームへ戻る
                        └───────┬───────┘               │
                                │ 「はじめる」               │
                                ↓                       │
                        ┌───────────────┐               │
                        │   CLI画面      │               │
                        │（新コマンド初回   │               │
                        │  ヒント込み）    │               │
                        └───────┬───────┘               │
                                │ 鍵情報を read で入手       │
                                ↓                       │
                        ┌───────────────┐               │
                        │ 鍵番号入力フォーム │               │
                        └───────┬───────┘               │
                        誤答 ↺   │ 正答                    │
                                ↓                       │
                        ┌───────────────┐               │
                        │  クリア演出画面   │───────────────┘
                        └───────────────┘
```

**重要**: ステージ1-1は専用の「チュートリアル画面」ではなく、他の全ステージと同じ「ステージ画面」の骨格（ストーリー→CLI→鍵番号フォーム→クリア演出）をそのまま使う。特別なコンポーネントを別途作らない。

---

## 1. タイトル画面

### 目的
ゲームの世界観を一目で伝え、ホーム（または初回はステージ1-1）へ導く。

### 表示要素
- タイトルロゴ（ターミナル風フォント、`$ _` のカーソル点滅演出）
- 「はじめる」ボタン
- バージョン表記（隅に小さく）

### 遷移
- 「はじめる」タップ
  - 初回起動 → ステージ1-1（ストーリー画面から通常のステージ画面フローに入る）
  - 2回目以降 → ホーム画面

---

## 2. チュートリアル（大枠）

### 設計方針
一括で5コマンドを教える専用画面は持たない。かわりに以下の二段構成にする。

1. **大枠のチュートリアル** = ステージ1-1を実際にプレイしてもらい、クリア直後に一度だけホーム画面の使い方を説明する
2. **各コマンドの説明** = 各ステージでそのコマンドが初めて必要になったタイミングで、CLI画面上にインラインヒントを出す（→ 2-3、5章）

### 2-1. ステージ1-1（大枠チュートリアルの実体）

使用するコマンドは `ls` `cd` `read` の3つのみ。`inspect` `run` `connect` はまだ使わせない。

**シナリオ**: `reception-pc` に `guest` としてログイン。受付に置き忘れられたメモを確認する。

| Step | 状況 | ヒント表示 | 操作 | 結果 |
|---|---|---|---|---|
| 1 | ログイン直後、何もわからない状態 | 「まずは `ls` で今いる場所を確認しよう」 | `ls` を選択 | `memo.txt` `staff/` が表示される |
| 2 | `staff/` が気になる（任意・本筋ではない寄り道） | ヒントなし | `cd` → `staff` を選択 | 拒否:「staff のみ入室可（あなた: guest）」→ 「壁がある」体験を先に刷り込む |
| 3 | `memo.txt` が読めそう | 「`read` で中身を確認しよう」 | `read` → `memo.txt` を選択 | 中身が出力される |
| 4 | 数字が判明 | 「見つけた数字を回答フォームに入力しよう」 | 鍵番号フォームに入力 | クリア |

**memo.txt の中身（例）**
```
--- memo.txt ---
今日の合言葉は 7008
（本当に大事な金庫の番号は root にしか
  見られない場所にあるらしい……）
```

末尾の一文は次ステージ（1-2、旧「ステージ1」＝`inspect`/`run` を初めて使う回）への伏線として機能させる。

**クリア演出（1-1用）**
- 気づき:「拒否されても、まず `ls` と `read` だけで進められる場所はある」
- ボタン:「つづける」→ ホーム画面ガイドへ

### 2-2. ホーム画面ガイド（ステージ1-1クリア直後のみ）

ステージ1-1クリア後、ホーム画面に遷移した際に一度だけ表示するオーバーレイ形式のガイド。以後は自動表示しない。

| 対象 | ハイライト内容 |
|---|---|
| ステージ一覧 | 「次はここから選んで挑戦しよう」 |
| クリア状態アイコン | 「クリアすると記録が残るよ」 |
| 進捗サマリ | 「全部で ◯ ステージあるよ」 |

- 「わかった」ボタンで閉じる
- スキップ不可にする必要はない（タップで即閉じてよい）

### 2-3. 各ステージ内チュートリアル（新コマンド初回ヒント）

CLI画面（4-2）の一部として、そのステージで**新しく使うことになるコマンド**が最初に登場するとき、コマンド選択欄の該当ボタンに軽いヒントを添える。

| コマンド | 想定初出ステージ | ヒント例 |
|---|---|---|
| `ls` `cd` `read` | ステージ1-1 | 2-1参照 |
| `inspect` `run` | ステージ1-2 | 「`inspect` でツールの仕様を見てみよう」「読めないなら、ツールに代わりに読ませてみよう」 |
| `status` | 早い段階の任意ステージ | 「今の自分の状態をまとめて見たいときは `status`」 |
| `connect` | パターン4系の初出ステージ | 「この端末からは弱そうな別端末に `connect` できそうだ」 |
| `back` | 詰みかけた最初のタイミング（条件発火） | 「一手戻したいときは `back`」 |
| `help` | 常時使用可能。専用ヒントは出さない（いつでも参照できるため） | — |

表示形式・消去条件は5章「新コマンド初回ヒント」に定義する。

---

## 3. ホーム画面

### 表示要素
- ステージ一覧（カード形式・スクロール）
- 各カードの表示情報

| 項目 | 内容 |
|---|---|
| ステージ番号/コードネーム | 例: `1-1`, `1-2` |
| タイトル | 例:「受付端末」 |
| クリア状態アイコン | 未挑戦 / 挑戦中 / クリア済み / ロック中 |
| ロック条件（ロック時のみ） | 例:「1-1 をクリアすると解放」 |

- 画面上部: クリア済みステージ数（例: `5 / 24`）※プレイヤーステータスの常時表示ではなく、進捗サマリとして1箇所に集約

### 遷移
- ステージカードをタップ（ロック中は不可）→ 該当ステージのストーリー画面
- ステージ1-1クリア直後の初回訪問のみ → ホーム画面ガイド（2-2）が自動表示される

---

## 4. ステージ画面

ステージ画面は3つの子画面（ストーリー表示 → CLI → 鍵番号入力フォーム）と、クリア演出で構成される。ステージ1-1も含め、全ステージがこの骨格を共有する。

### 4-1. ストーリー表示画面

#### 表示要素
- ステージタイトル
- 導入ストーリーテキスト（状況設定・侵入対象のホスト名・自分の権限・何を暴きたいのかを匂わせる）
- 「はじめる」ボタン

#### モックアップ
```
──────────────────────────
  1-2 「受付端末・裏側」

  受付のメモにあった一文が気になる。
  金庫の本当の番号は、root しか
  読めないファイルの中にあるらしい。

              [ はじめる ]
──────────────────────────
```

#### 遷移
- 「はじめる」→ CLI画面

---

### 4-2. CLI画面（ゲームプレイ本体）

#### 設計方針（重要）
Overview.md の「自由入力ゼロ」に従い、**キーボードでコマンド文字列をタイプさせない**。プレイヤーは「コマンド」→「対象」の順に選択肢をタップして文を組み立てる。組み立てられたコマンドは実際にターミナルで打ったかのように出力ログへ追記される。

#### 画面構成
```
┌───────────────────────────────┐
│ 出力ログ（スクロール領域）              │
│                                 │
│ $ ls                           │
│ memo.txt  printer               │
│                                 │
│ $ read /root/note              │
│ 拒否: root のみ読み取り可（あなた: guest）│
│                                 │
│ $ inspect printer               │
│ 所有者: root / 実行可: 全員         │
│ 動作: run printer <ファイル> で表示    │
│                                 │
├───────────────────────────────┤
│ コマンド選択                        │
│ [ ls ][inspect★][read][cd]       │
│ [connect][run★][status][help][back]│
│  ★=このステージで初出（2-3のヒント表示） │
├───────────────────────────────┤
│ 対象選択（コマンド選択後に表示）           │
│ [ memo.txt ] [ printer ]         │
└───────────────────────────────┘
```

#### 操作フロー（`run` の例）
1. コマンド行から `run` を選択
2. 「対象を選択」欄に、現在地の `ls` 結果からツールが候補表示される → `printer` を選択
3. `run` は引数が必要なため、続けて「引数を選択」欄が表示される → ファイル/ディレクトリ一覧（現在見えている範囲、または既知のパス）から `/root/note` を選択
4. 確定すると出力ログに `$ run printer /root/note` として追記され、実行結果が続く

#### 補助要素
- `help` 選択時: 現在使えるコマンド一覧と一言説明をログに出力（別画面には遷移しない）
- `back` 選択時: 直前の1手を取り消し、対応するログ行を灰色（取り消し済み）表示に変える
- 拒否メッセージはログ内にインラインで表示（Overview.md 記載の書式に統一）
  ```
  拒否: root のみ入室可（あなた: guest）
  ```
- 常時ステータスバーは置かない。現在地・権限などのまとまった情報が必要な場合のみ `status` を選択して都度ログに出力する
- 新コマンド初回ヒント（2-3）はこの画面のコマンド選択欄に付随する形で表示する

#### 遷移
- ゴールとなる情報（鍵番号など）を `read`/`run` 経由で入手すると、ログ末尾に「回答する」ボタンが出現 → 鍵番号入力フォームへ

---

### 4-3. 鍵番号入力フォーム

#### 位置づけ
CLI操作そのものは選択式（自由入力ゼロ）だが、**ステージのクリア条件の最終回答**はプレイヤーが実際に見つけた番号を入力する行為そのものが本人の理解確認になるため、この画面に限り数値入力を許可する例外とする。

#### 表示要素
- 「見つけた番号を入力してください」等の案内文
- 数値入力欄（テンキー形式を想定。桁数はステージのJSON定義に依存）
- 「回答する」ボタン
- 誤答時: 「番号が違います。もう一度確認しよう」のようなヒントレスな短いエラー表示（正解への直接的なヒントは出さない）＋ CLI画面へ戻る導線

#### モックアップ
```
──────────────────────────
  金庫の暗証番号を入力

        [ 4 ][ 9 ][ 2 ][ 1 ]

          [ 回答する ]

  ※ CLI画面に戻って確認する場合は
    「戻る」を選択
──────────────────────────
```

#### 判定
- 入力値がステージ定義の `goal` と一致 → クリア演出画面
- 不一致 → エラー表示、再入力可（回数制限なし）

#### 遷移
- 正答 → クリア演出画面
- 「戻る」→ CLI画面（ログはそのまま保持）

---

### 4-4. クリア演出画面

#### 表示要素
- 「クリア」演出（テキストアニメーション想定）
- そのステージの「気づき」テキスト（Overview.md のサンプル盤面にある一言まとめ。例:「自分は読めないが、ツールに代行させたら読めた」）
- 実行したコマンド手数（任意表示。スコアリングはしないが、手数の短さを本人が振り返れる情報として）
- ボタン: 「次のステージへ」「ホームへ戻る」

#### 遷移
- 「次のステージへ」→ 次ステージのストーリー画面（ロックされていれば表示しない）
- 「ホームへ戻る」→ ホーム画面
- ステージ1-1クリア時のみ、「つづける」→ ホーム画面ガイド（2-2）

---

## 5. 共通UI要素

| 要素 | 仕様 |
|---|---|
| プロンプト表記 | 常に `$` のみ。ステージ・端末が変わっても記号は変えない |
| 拒否メッセージ | 都度ログにインライン表示。ポップアップ・モーダルにしない（Overview.mdの「常時ステータスバーを持たない」思想を踏襲し、割り込みUIを増やさない） |
| ステータス確認 | 専用バーを持たず、`status` コマンドの出力としてのみログに表示 |
| ボタン活性/非活性 | 選択不可な対象（権限的に無意味な組み合わせ等）はグレーアウトではなく、選択自体は許可し、実行後に拒否メッセージで返す（「拒否される体験」自体がゲーム性のため） |
| 新コマンド初回ヒント | そのステージで初めて選択可能になったコマンドのボタンに「NEW」バッジ＋一言吹き出しを添える。該当コマンドを一度選択したら、以後そのステージ内・以降のステージでは表示しない |

---

## 6. ステージ1-2 詳細仕様（具体例：パターン1-A「受付端末・裏側」）

Overview.md のサンプル盤面（Lv1）に対応する画面遷移の実例。ステージ1-1の続きとして、`inspect` と `run` を初めて使わせる回。

1. **ストーリー画面**
   「受付のメモにあった一文が気になる。金庫の本当の番号は、root しか読めないファイルの中にあるらしい。」→「はじめる」
2. **CLI画面**
   - `ls` → `memo.txt` `printer` が見える
   - `read` → `/root/note` を選択 → 拒否される
   - `inspect`★（初出ヒント表示）→ `printer` を選択 → 仕様が判明（root所有・全員実行可・run printer <ファイル> で表示）
   - `run`★（初出ヒント表示）→ `printer` を選択 → 引数に `/root/note` を選択 → 内容が出力される
     ```
     --- /root/note ---
     金庫の暗証番号は 4921
     ```
   - ログ末尾に「回答する」ボタン出現
3. **鍵番号入力フォーム**
   - `4921` を入力 → 「回答する」
4. **クリア演出画面**
   - 「クリア」
   - 気づき:「自分は読めないが、ツールに代行させたら読めた」
   - 「次のステージへ」/「ホームへ戻る」

---

## 未決事項

- [ ] ステージ1-1の `memo.txt` に書く伏線テキストの最終文言（1-2との接続を自然にする）
- [ ] 新コマンド初回ヒントの表示トリガー（「ボタンが選択可能になった瞬間からずっと」か「一定時間 or 一定手数で自動的に消す」か）
- [ ] `help` に初回ヒントが不要という判断でよいか（常時参照可能なため現状は不要としているが要検証）
- [ ] ホーム画面ガイド（2-2）を後からもう一度見たい場合の導線の要否
- [ ] `run` の引数選択で「まだ見つけていないパス」をどこまで候補に出すか（既知のパスのみ / 総当たり的に見せない）
- [ ] 鍵番号フォームの桁数・入力形式（数値固定か、英数字を含むステージがあるか）
- [ ] 誤答時のペナルティ有無（Overview.mdの「数値強化・グラインドNG」方針に沿い、基本はペナルティなしを想定）
- [ ] 複数端末ステージ（パターン4・5）でのCLI画面上の「現在どの端末にいるか」の見せ方（常時ステータスバー無しの制約内での表現）
